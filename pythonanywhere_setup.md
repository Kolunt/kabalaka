# Инструкция по развертыванию на PythonAnywhere

## Шаг 1: Клонирование репозитория

1. Откройте **Bash консоль** на PythonAnywhere
2. Перейдите в домашнюю директорию:
   ```bash
   cd ~
   ```
3. Клонируйте репозиторий с GitHub:
   ```bash
   git clone https://github.com/Kolunt/tg-alarm-bot.git alarm-bot
   ```
   Или если хотите использовать другое имя директории:
   ```bash
   git clone https://github.com/Kolunt/tg-alarm-bot.git
   ```
   (в этом случае директория будет называться `tg-alarm-bot`)

4. Перейдите в директорию проекта:
   ```bash
   cd ~/alarm-bot
   ```
   (или `cd ~/tg-alarm-bot` если использовали вторую команду)

5. Убедитесь, что все файлы на месте:
   ```bash
   ls -la
   ```
   Должны быть видны все файлы проекта, включая:
   - Все `.py` файлы
   - `requirements.txt`
   - `locales/` директория с файлами переводов
   - `README.md`, `CHANGELOG.md` и другие файлы документации

## Шаг 2: Установка зависимостей

1. Убедитесь, что вы находитесь в директории проекта:
   ```bash
   cd ~/alarm-bot
   ```
2. Установите зависимости:
   ```bash
   pip3.10 install --user -r requirements.txt
   ```
   
   Если возникают проблемы, попробуйте:
   ```bash
   pip3.10 install --user --upgrade pip
   pip3.10 install --user -r requirements.txt
   ```

## Шаг 3: Настройка переменных окружения

### Вариант 1: Через файл .env (базовая настройка)

1. Создайте файл `.env` в корне проекта:
   ```bash
   cd ~/alarm-bot
   nano .env
   ```
   Или используйте редактор в **Files** tab на PythonAnywhere

2. Добавьте минимальные переменные:
   ```env
   TELEGRAM_BOT_TOKEN=ваш_токен_бота
   FLASK_SECRET_KEY=случайная_строка_для_безопасности
   DATABASE_URL=sqlite:///calendar_bot.db
   ```

3. Сохраните файл:
   - В nano: `Ctrl+O` (сохранить), затем `Enter`, затем `Ctrl+X` (выйти)
   - В редакторе Files tab: просто нажмите **Save**

### Вариант 2: Настройка через админ-панель (рекомендуется)

После первого запуска вы сможете настроить все параметры через веб-интерфейс админ-панели:
- Telegram Bot Token
- Google OAuth credentials
- Yandex OAuth credentials
- Настройки планировщика

## Шаг 4: Инициализация базы данных

В Bash консоли выполните:
```bash
python3.10 init_db.py
```

Это создаст все необходимые таблицы в базе данных.

## Шаг 5: Создание администратора

В Bash консоли выполните:
```bash
python3.10 create_admin.py
```

Введите:
- **Username**: имя пользователя для входа в админ-панель
- **Password**: пароль для входа

⚠️ **Важно**: Это будет единственный администратор системы. Запомните эти данные!

## Шаг 6: Настройка Web App

1. Перейдите в раздел **Web**
2. Создайте новый Web app или используйте существующий
3. В разделе **Code** укажите:
   - **Source code**: `/home/yourusername/alarm-bot`
   - **Working directory**: `/home/yourusername/alarm-bot`
   - **WSGI configuration file**: выберите файл или создайте новый

4. **Проверьте путь к проекту**:
   
   Откройте **Bash консоль** и выполните:
   ```bash
   cd ~
   pwd
   ls -la
   ```
   
   Проверьте, где находятся файлы проекта (`app.py`, `bot.py` и т.д.):
   - Если файлы в `/home/kabalaka/` (прямо в домашней директории), используйте путь `/home/kabalaka`
   - Если файлы в `/home/kabalaka/alarm-bot/`, используйте путь `/home/kabalaka/alarm-bot`
   
   Если проект ещё не клонирован, выполните:
   ```bash
   cd ~
   git clone https://github.com/Kolunt/tg-alarm-bot.git alarm-bot
   cd alarm-bot
   pwd  # Запомните этот путь!
   ```

5. Отредактируйте WSGI файл (например, `/var/www/yourusername_pythonanywhere_com_wsgi.py`):
   
   ⚠️ **ВАЖНО**: 
   - Убедитесь, что нет лишних пробелов или табов в начале строк!
   - Замените `yourusername` на ваш реальный username (например, `kabalaka`)
   - **Замените путь** на реальный путь, где находятся файлы проекта (проверьте через `ls -la` в Bash консоли)
   
   **Если проект в `/home/kabalaka/` (файлы прямо в домашней директории):**
   ```python
   import sys
   import os

   # Добавьте путь к проекту
   path = '/home/kabalaka'
   if path not in sys.path:
       sys.path.insert(0, path)

   # Установите рабочую директорию
   os.chdir(path)

   # Импортируйте приложение
   from app import app as application
   ```
   
   **Если проект в `/home/kabalaka/alarm-bot/`:**
   ```python
   import sys
   import os

   # Добавьте путь к проекту
   path = '/home/kabalaka/alarm-bot'
   if path not in sys.path:
       sys.path.insert(0, path)

   # Установите рабочую директорию
   os.chdir(path)

   # Импортируйте приложение
   from app import app as application
   ```
   
   ⚠️ **Примечание**: 
   - На PythonAnywhere не нужно `if __name__ == "__main__"` в WSGI файле. Просто `application` должен быть доступен.
   - Если получаете ошибку `FileNotFoundError`, проверьте правильность пути через Bash консоль: `ls -la /home/yourusername/` и найдите, где находится `app.py`

5. Сохраните изменения и нажмите **Reload** в разделе Web

## Шаг 7: Настройка бота для бесплатного тарифа

⚠️ **Важно**: На бесплатном тарифе PythonAnywhere **Always-on tasks недоступны**. 

### Ограничения бесплатного тарифа

- ❌ Нет Always-on tasks (постоянно работающих задач)
- ✅ Есть Scheduled tasks (запланированные задачи)
- ⚠️ Scheduled tasks выполняются только если Web app активен
- ⚠️ Web app может "засыпать" после неактивности

### Варианты работы бота на бесплатном тарифе

#### Вариант 1: Использование внешних cron-сервисов (рекомендуется)

Используйте бесплатные внешние сервисы для периодического вызова endpoints:

1. **Настройте внешний cron-сервис** (например, [cron-job.org](https://cron-job.org) или [EasyCron](https://www.easycron.com)):
   
   **Для пробуждения Web app и проверки событий:**
   - URL: `https://yourusername.pythonanywhere.com/cron/run-all`
   - Интервал: каждые 5-10 минут
   - Метод: GET
   
   **Для запуска бота на обработку сообщений:**
   - URL: `https://yourusername.pythonanywhere.com/cron/run-bot`
   - **Важно**: Для быстрой работы бота рекомендуется вызывать этот endpoint каждые 30-60 секунд
   - Интервал: каждые 5-10 минут
   - Метод: GET
   
   **Для пробуждения Web app (чтобы он не засыпал):**
   - URL: `https://yourusername.pythonanywhere.com/cron/wake`
   - Интервал: каждые 5 минут
   - Метод: GET

2. **Доступные endpoints:**
   - `/cron/wake` - пробуждение Web app
   - `/cron/check-events` - проверка событий календарей
   - `/cron/check-broadcasts` - проверка отложенных рассылок
   - `/cron/run-bot` - запуск бота для обработки сообщений
   - `/cron/run-all` - запуск всех задач (события + рассылки)

3. **Преимущества:**
   - ✅ Работает на бесплатном тарифе
   - ✅ Можно настроить любой интервал (не только раз в сутки)
   - ✅ Web app будет "пробуждаться" и оставаться активным
   - ✅ Все задачи выполняются автоматически

#### Вариант 2: Использование Scheduled Task (раз в сутки)

Если нет возможности использовать внешние cron-сервисы:

1. Перейдите в раздел **Tasks**
2. Создайте **Scheduled task** для бота:
   - **Command**: 
     ```bash
     cd /home/yourusername/alarm-bot && python3.10 -c "import requests; requests.get('https://yourusername.pythonanywhere.com/cron/run-all')"
     ```
   - **Hour**: выберите удобное время (например, `9`)
   - **Minute**: `0`
   - **Description**: Daily bot tasks
   - ⚠️ **Примечание**: Задачи будут выполняться только раз в сутки. Для более частой проверки используйте Вариант 1.

#### Вариант 2: Использование Webhook (для продвинутых пользователей)

Можно настроить webhook вместо polling, но это требует дополнительной настройки и стабильного HTTPS.

#### Вариант 3: Платный тариф или другой хостинг

Для полноценной работы бота с постоянным polling рекомендуется:
- Перейти на платный тариф PythonAnywhere (есть Always-on tasks)
- Или использовать другой хостинг (VPS, Heroku, Railway и т.д.)

## Шаг 8: Настройка планировщика

### Для бесплатного тарифа: Использование внешних cron-сервисов

⚠️ **Важно**: На бесплатном тарифе Scheduled tasks можно установить только **раз в сутки**. Для более частой работы используйте внешние cron-сервисы.

#### Настройка через cron-job.org (рекомендуется)

1. Зарегистрируйтесь на [cron-job.org](https://cron-job.org) (бесплатно)
2. Создайте новую задачу:
   - **Title**: Kabalaka - Check events and broadcasts
   - **Address**: `https://yourusername.pythonanywhere.com/cron/run-all`
   - **Schedule**: выберите интервал (например, каждые 5 минут)
   - **Request method**: GET
   - Нажмите **Create cronjob**

3. (Опционально) Создайте задачу для пробуждения Web app:
   - **Title**: Kabalaka - Wake up
   - **Address**: `https://yourusername.pythonanywhere.com/cron/wake`
   - **Schedule**: каждые 5 минут
   - **Request method**: GET

4. (Опционально) Создайте задачу для запуска бота:
   - **Title**: Kabalaka - Run bot
   - **Address**: `https://yourusername.pythonanywhere.com/cron/run-bot`
   - **Schedule**: каждые 10 минут
   - **Request method**: GET

#### Альтернативные cron-сервисы

- [EasyCron](https://www.easycron.com) - бесплатный тариф: до 2 задач
- [UptimeRobot](https://uptimerobot.com) - бесплатный тариф: мониторинг + HTTP requests
- [Healthchecks.io](https://healthchecks.io) - бесплатный тариф для мониторинга

### Для платного тарифа: Использование Scheduled Tasks

Если у вас платный тариф PythonAnywhere, можете использовать Scheduled tasks:

1. Перейдите в раздел **Tasks**
2. Создайте **Scheduled task** для проверки событий:
   - **Command**: 
     ```bash
     cd /home/yourusername/alarm-bot && python3.10 -c "import requests; requests.get('https://yourusername.pythonanywhere.com/cron/check-events')"
     ```
   - **Hour**: `*` (каждый час)
   - **Minute**: `*/5` (каждые 5 минут)
   - **Description**: Check calendar events

3. Создайте **Scheduled task** для проверки рассылок:
   - **Command**: 
     ```bash
     cd /home/yourusername/alarm-bot && python3.10 -c "import requests; requests.get('https://yourusername.pythonanywhere.com/cron/check-broadcasts')"
     ```
   - **Hour**: `*`
   - **Minute**: `*/1` (каждую минуту) или `*` (каждые 30 секунд, если сервис поддерживает)
   - **Важно**: Для быстрой работы бота рекомендуется вызывать `/cron/run-bot` каждые 30-60 секунд

⚠️ **Примечание**: Интервал проверки событий можно настроить через админ-панель в разделе "Настройки" → "Планировщик"

## Шаг 9: Настройка OAuth Redirect URIs

### Google Calendar

1. Откройте [Google Cloud Console](https://console.cloud.google.com/)
2. Перейдите в ваш проект
3. **APIs & Services** → **Credentials**
4. Откройте ваш OAuth 2.0 Client ID
5. В разделе **Authorized redirect URIs** добавьте:
   ```
   https://yourusername.pythonanywhere.com/callback/google
   ```
6. Сохраните изменения

### Yandex Calendar

1. Откройте [Yandex OAuth](https://oauth.yandex.ru/)
2. Откройте ваше приложение
3. В разделе **Callback URI** добавьте:
   ```
   https://yourusername.pythonanywhere.com/callback/yandex
   ```
4. Сохраните изменения

## Шаг 10: Настройка через админ-панель

После запуска Web app:

1. Откройте в браузере: `https://yourusername.pythonanywhere.com/`
2. Войдите в админ-панель используя созданные учётные данные
3. Перейдите в **Настройки** → **Основные настройки**
4. Заполните:
   - **Telegram Bot Token** (если не был указан в `.env`)
   - **Google Client ID** и **Google Client Secret**
   - **Google Redirect URI**: `https://yourusername.pythonanywhere.com/callback/google`
   - **Yandex Client ID** и **Yandex Client Secret**
   - **Yandex Redirect URI**: `https://yourusername.pythonanywhere.com/callback/yandex`
5. Сохраните настройки

6. Перейдите в **Настройки** → **Планировщик**:
   - Настройте интервал проверки событий (рекомендуется: 5 минут)
   - Включите/выключите планировщик

7. Перейдите в **Настройки** → **Управление ботом**:
   - Проверьте статус подключения к Telegram боту
   - Убедитесь, что бот работает

## Проверка работы

### 1. Проверка Web App

1. Откройте: `https://yourusername.pythonanywhere.com/`
2. Должно произойти перенаправление на `/admin/login`
3. Войдите в админ-панель
4. Проверьте health endpoint: `https://yourusername.pythonanywhere.com/health`
   - Должен вернуть: `{"status": "ok"}`

### 2. Проверка бота

⚠️ **На бесплатном тарифе**: Бот работает периодически, поэтому ответ может быть с задержкой.

1. Найдите вашего бота в Telegram: [@KabalakaBot](https://t.me/KabalakaBot)
2. Отправьте команду `/start`
3. Бот должен ответить в течение нескольких минут (когда выполнится следующая Scheduled task)
4. Если бот не отвечает долго, проверьте:
   - Настроен ли Scheduled task для бота
   - Активен ли Web app
   - Правильный ли токен бота

### 3. Проверка админ-панели

Админ-панель доступна по адресу: `https://yourusername.pythonanywhere.com/admin/login`

**Доступные разделы:**
- 📊 **Дашборд**: Статистика системы
- 👥 **Пользователи**: Список всех пользователей и их календари
- 📨 **Рассылки**: Создание и управление массовыми рассылками
- ⚙️ **Настройки**:
  - Основные настройки (OAuth credentials, токены)
  - Планировщик (интервал проверки событий)
  - Управление ботом (статус, перезапуск)

### 4. Проверка OAuth

1. В Telegram боте нажмите кнопку подключения календаря
2. Должно открыться окно авторизации
3. После авторизации должно появиться сообщение об успешном подключении

## Важные замечания

### Ограничения бесплатного тарифа

- ❌ **Always-on tasks недоступны** - нет постоянно работающих задач
- ⚠️ Web app может "засыпать" после неактивности
- ⚠️ Scheduled tasks выполняются только если Web app активен
- ⚠️ Ограничения на количество запросов к API
- ⚠️ Бот не может работать постоянно в режиме polling

### Рекомендации для бесплатного тарифа

- ✅ **Используйте внешние cron-сервисы** для периодического вызова endpoints (обход ограничения "раз в сутки")
- ✅ Настройте `/cron/run-all` на вызов каждые 5-10 минут через cron-job.org
- ✅ Настройте `/cron/wake` на вызов каждые 5 минут для поддержания Web app активным
- ✅ Используйте встроенный **BackgroundScheduler** в `app.py` (работает пока Web app активен)
- ✅ Регулярно проверяйте логи в разделе **Web** → **Error log**
- ✅ Настройте все параметры через админ-панель (удобнее, чем редактировать `.env`)
- ⚠️ Для полноценной работы с постоянным polling рассмотрите переход на платный тариф или другой хостинг

### Рекомендации для платного тарифа

Если у вас платный тариф PythonAnywhere:
- ✅ Используйте **Always-on task** для постоянной работы бота
- ✅ Настройте **Scheduled tasks** для проверки событий (более надёжно, чем встроенный планировщик)

### Структура файлов на сервере

```
/home/yourusername/alarm-bot/
├── app.py                    # Flask приложение (запускается через Web app)
├── run_bot.py                # Скрипт бота (запускается через Scheduled task на бесплатном тарифе)
├── database.py
├── bot.py
├── admin_panel.py
├── scheduler.py
├── broadcast_sender.py       # Модуль рассылок
├── calendar_google.py
├── calendar_yandex.py
├── config.py
├── i18n.py                   # Модуль локализации
├── locales/                  # Файлы переводов
│   ├── en.json
│   ├── ru.json
│   └── es.json
├── calendar_bot.db           # База данных SQLite
├── .env                      # Переменные окружения
└── requirements.txt
```

## Решение проблем

### Бот не отвечает

**На бесплатном тарифе:**
1. Проверьте, что внешние cron-задачи настроены и работают (cron-job.org и т.д.)
2. Проверьте, что endpoints отвечают:
   - `https://yourusername.pythonanywhere.com/cron/wake` - должен вернуть `{"status": "awake"}`
   - `https://yourusername.pythonanywhere.com/cron/run-all` - должен вернуть `{"status": "success"}`
3. Убедитесь, что Web app активен (проверьте в разделе **Web**)
4. Проверьте логи в разделе **Web** → **Error log**
5. Убедитесь, что токен бота правильный (проверьте в админ-панели)
6. Проверьте статус бота в админ-панели: **Настройки** → **Управление ботом**

**На платном тарифе:**
1. Проверьте, что **Always-on task** запущен (зелёный индикатор)
2. Проверьте логи в разделе **Tasks** → нажмите на задачу → **View output**
3. Убедитесь, что токен бота правильный (проверьте в админ-панели)
4. Проверьте статус бота в админ-панели: **Настройки** → **Управление ботом**

**Важно**: На бесплатном тарифе бот не работает постоянно, а запускается периодически. Это нормальное поведение для бесплатного тарифа.

### OAuth не работает

1. Проверьте redirect URIs в настройках OAuth приложений:
   - Google: `https://yourusername.pythonanywhere.com/callback/google`
   - Yandex: `https://yourusername.pythonanywhere.com/callback/yandex`
2. Убедитесь, что Web app доступен по HTTPS
3. Проверьте настройки в админ-панели: **Настройки** → **Основные настройки**
4. Проверьте логи в разделе **Web** → **Error log**

### События не проверяются

1. Проверьте, что внешние cron-задачи настроены (cron-job.org и т.д.)
2. Проверьте, что `/cron/run-all` или `/cron/check-events` вызываются регулярно
3. Убедитесь, что Web app активен
4. Проверьте настройки планировщика в админ-панели: **Настройки** → **Планировщик**
5. Проверьте логи в разделе **Web** → **Error log**
6. Проверьте, что встроенный BackgroundScheduler работает (он активен пока Web app не спит)

### Рассылки не отправляются

1. Проверьте статус рассылки в админ-панели: **Рассылки** → выберите рассылку
2. Проверьте историю отправок в деталях рассылки
3. Убедитесь, что бот работает (проверьте в **Настройки** → **Управление ботом**)
4. Проверьте логи в разделе **Web** → **Error log**

### Админ-панель не открывается

1. Проверьте, что Web app запущен (зелёный индикатор)
2. Проверьте логи в разделе **Web** → **Error log**
3. Убедитесь, что база данных инициализирована: `python3.10 init_db.py`
4. Убедитесь, что администратор создан: `python3.10 create_admin.py`

### Ошибки при запуске

1. Проверьте, что все зависимости установлены: `pip3.10 list`
2. Проверьте версию Python: `python3.10 --version` (должна быть 3.10+)
3. Проверьте логи в разделе **Web** → **Error log**
4. Проверьте логи Scheduled task в разделе **Tasks** (или Always-on task на платном тарифе)

## Обновление проекта

При обновлении проекта:

1. Откройте **Bash консоль** и перейдите в директорию проекта:
   ```bash
   cd ~/alarm-bot
   ```

2. Получите последние изменения с GitHub:
   ```bash
   git pull origin main
   ```
   Или если используете другую ветку:
   ```bash
   git pull origin <branch_name>
   ```

3. Обновите зависимости (если изменился `requirements.txt`):
   ```bash
   pip3.10 install --user --upgrade -r requirements.txt
   ```

4. Если изменилась структура БД, может потребоваться миграция (проверьте `CHANGELOG.md`)

5. Перезагрузите Web app: **Web** → **Reload**

6. Перезапустите Scheduled task (или Always-on task на платном тарифе): **Tasks** → остановите и запустите заново

### Проверка текущей версии

Чтобы узнать текущую версию проекта:
```bash
cd ~/alarm-bot
cat VERSION
```

Или посмотрите в `CHANGELOG.md` последнюю версию.

### Проверка статуса репозитория

Чтобы проверить, есть ли новые изменения на GitHub:
```bash
cd ~/alarm-bot
git fetch origin
git status
```

Это покажет, отстаёт ли ваша локальная версия от удалённой.

## Безопасность

⚠️ **Важные рекомендации:**

1. Используйте сложный пароль для администратора
2. Не публикуйте `.env` файл в открытом доступе
3. Регулярно проверяйте логи на подозрительную активность
4. Обновляйте зависимости для исправления уязвимостей
5. Используйте HTTPS (автоматически на PythonAnywhere)

## Дополнительная информация

- **Версионирование**: Проект использует семантическое версионирование (см. `VERSION` и `CHANGELOG.md`)
- **Локализация**: Поддерживаются языки: English, Русский, Español
- **База данных**: SQLite (файл `calendar_bot.db` в корне проекта)
- **Логи**: Доступны в разделе **Web** → **Error log** и **Tasks** → **View output**

## Поддержка

Если возникли проблемы:
1. Проверьте раздел "Решение проблем" выше
2. Проверьте логи приложения
3. Убедитесь, что все шаги инструкции выполнены правильно
4. Проверьте документацию в `README.md` и `CHANGELOG.md`
