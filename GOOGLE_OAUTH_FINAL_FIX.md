# Финальное решение проблемы "OAuth client was not found"

## 🔍 Диагностика

Ошибка `401: invalid_client` с сообщением "The OAuth client was not found" означает, что:
- ✅ Authorization URL создается успешно (credentials правильного формата)
- ❌ Но Google не находит OAuth приложение с таким Client ID

## 🎯 Причина

**Client ID `730550260978-jlk79esc3lau4q4m7fnhndeiuc9hcngi.apps.googleusercontent.com` не существует в Google Cloud Console** (или существует в другом проекте).

## ✅ Решение: Создайте новое OAuth приложение

### Шаг 1: Откройте Google Cloud Console

1. Перейдите: https://console.cloud.google.com/
2. **ВАЖНО:** Выберите правильный проект (или создайте новый)

### Шаг 2: Настройте OAuth Consent Screen (если нужно)

1. Перейдите: **APIs & Services** → **OAuth consent screen**
2. Если еще не настроен:
   - **User Type**: External
   - **App name**: Calendar Bot
   - **User support email**: ваш email
   - **Developer contact**: ваш email
   - Нажмите **Save and Continue**
   - В **Scopes**: нажмите **Add or Remove Scopes**
     - Найдите и добавьте: `https://www.googleapis.com/auth/calendar.readonly`
     - Нажмите **Update** → **Save and Continue**
   - В **Test users**: нажмите **Add Users**
     - Добавьте: `kolunt@gmail.com`
     - Нажмите **Add** → **Save and Continue**
   - Нажмите **Back to Dashboard**

### Шаг 3: Создайте OAuth 2.0 Client ID

1. Перейдите: **APIs & Services** → **Credentials**
2. Нажмите: **Create Credentials** → **OAuth client ID**
3. Если появится запрос на настройку OAuth consent screen - выполните Шаг 2
4. В форме создания:
   - **Application type**: Web application
   - **Name**: Calendar Bot
   - **Authorized redirect URIs**: 
     ```
     http://localhost:5000/callback/google
     ```
   - ⚠️ **ТОЧНО такой URI, без пробелов!**
5. Нажмите **Create**

### Шаг 4: Скопируйте новые credentials

После создания вы увидите:
- **Your Client ID** (длинная строка, заканчивается на `.apps.googleusercontent.com`)
- **Your Client Secret** (строка вида `GOCSPX-...`)

⚠️ **ВАЖНО:** Скопируйте их сразу, Client Secret показывается только один раз!

### Шаг 5: Обновите .env файл

Откройте `.env` и замените:

```
GOOGLE_CLIENT_ID=новый_client_id_из_google_console
GOOGLE_CLIENT_SECRET=новый_client_secret_из_google_console
GOOGLE_REDIRECT_URI=http://localhost:5000/callback/google
```

⚠️ **Убедитесь:**
- Нет лишних пробелов
- Нет переносов строк
- Client ID и Client Secret скопированы полностью

### Шаг 6: Перезапустите Flask сервер

После обновления `.env` перезапустите Flask сервер.

### Шаг 7: Проверьте

1. Попробуйте снова подключить Google Calendar через бота
2. Ошибка должна исчезнуть

## 🔄 Альтернатива: Проверьте существующее приложение

Если вы уверены, что OAuth приложение существует:

1. В Google Cloud Console: **APIs & Services** → **Credentials**
2. Найдите OAuth 2.0 Client ID, который начинается с `730550260978-...`
3. Нажмите на него для редактирования
4. Проверьте:
   - ✅ Client ID точно совпадает с тем, что в `.env`
   - ✅ Client Secret точно совпадает (если забыли - создайте новый)
   - ✅ В "Authorized redirect URIs" указан: `http://localhost:5000/callback/google`
5. Если что-то не совпадает - исправьте и сохраните

## 📋 Чек-лист

- [ ] OAuth приложение создано в Google Cloud Console
- [ ] Client ID скопирован правильно (без пробелов)
- [ ] Client Secret скопирован правильно (без пробелов)
- [ ] Redirect URI в Google Console: `http://localhost:5000/callback/google`
- [ ] Redirect URI в `.env`: `http://localhost:5000/callback/google`
- [ ] Email `kolunt@gmail.com` добавлен в Test users
- [ ] Google Calendar API включен
- [ ] Flask сервер перезапущен после изменений

## ⚠️ Если ничего не помогает

1. Создайте **НОВЫЙ** проект в Google Cloud Console
2. Создайте **НОВОЕ** OAuth приложение
3. Используйте **НОВЫЕ** credentials
4. Обновите `.env` файл




